# 🔐 Pysocknet (AES + RSA)

Небольшая Python-библиотека для защищённого и удобного в создании сетевого взаимодействия по **TCP и UDP**  
с поддержкой:

- 🔑 **RSA-обмена ключами**
- 🔒 **AES-GCM шифрования**
- 📦 **пакетной (chunked) передачи данных**
- 📡 **TCP и UDP клиентов/серверов**
- 📁 передачи бинарных данных и строк

Проект подходит для создания собственных защищённых протоколов поверх сокетов.

---

## 📦 Возможности

### 🔐 Безопасность
- **RSA (1500 бит)** используется для безопасного обмена AES-ключом
- **AES-GCM (256 бит)** — симметричное шифрование с аутентификацией
- Проверка целостности сообщений (GCM tag)

### 🌐 TCP
- TCP Client / Server
- Защищённый handshake (`ratp_handshake`)
- Шифрованная отправка и получение данных
- Автоматическая передача больших сообщений по чанкам
- Поддержка `str` и `bytes`

### 📡 UDP
- UDP Client / Server
- Быстрая передача сообщений
- Подходит для простых запросов без шифрования

---

## 📁 Структура проекта

Проект представляет собой **один Python-модуль**, содержащий следующие классы:

### 🔐 Криптография
- `AESManager`

### 🌐 TCP
- `TCPClientConnection`
- `TCPServerConnection`

### 📡 UDP
- `UDPClientConnection`
- `UDPServerConnection`

---

## ⚙️ Установка зависимостей

```bash
pip install rsa cryptography
```
### 📥 Импорт

Скопируйте файл в ваш проект и импортируйте нужные классы:

```python
from pysocknet import (
    TCPClientConnection,
    TCPServerConnection,
    UDPClientConnection,
    UDPServerConnection
)
```
---

### 🧠 Особенности реализации

- Используется ast.literal_eval для безопасного парсинга данных

- AES-ключ хранится на сервере для каждого соединения

- RSA-ключи создаются на стороне клиента

- Поддержка автоматического удаления ключей при разрыве соединения

--- 

### ⚠️ Ограничения

- Не является полноценным TLS-аналогом

- UDP-соединения не зашифрованы

- Не рекомендуется для production без доработок

---

**Проект разрабатывается одним человеком - LimneticGG. По любым вопросам и предложениям пишите мне в Telegram (@limneticgg)**

